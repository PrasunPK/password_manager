<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="me.opens.password_manager">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".PasswordManagerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_lock_open_black"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_lock_open_black"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".activity.DisplayCredentialsActivity"
            android:parentActivityName=".activity.BaseActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.BaseActivity" />
        </activity>

        <activity
            android:name=".activity.RevealCredentialActivity"
            android:parentActivityName=".activity.DisplayCredentialsActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.DisplayCredentialsActivity" />
        </activity>

        <activity
            android:name=".activity.PassCodeActivity"
            android:parentActivityName=".activity.BaseActivity"
            android:label="@string/title_activity_login"
            android:noHistory="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.BaseActivity" />
        </activity>

        <activity
            android:name=".activity.RegistrationActivity"
            android:parentActivityName=".activity.BaseActivity"
            android:label="@string/title_activity_login"
            android:noHistory="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.BaseActivity" />
        </activity>


        <activity
            android:name=".activity.BaseActivity"
            android:label="@string/title_activity_base"
            android:noHistory="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>